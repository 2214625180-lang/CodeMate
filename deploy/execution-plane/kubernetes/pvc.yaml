apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codemate-sandbox-workspaces
  namespace: codemate-sandbox
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 20Gi
